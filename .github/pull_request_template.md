## What does this change?

## Why?

## How was it verified?

- [ ] `npm run lint` passes
- [ ] `npm test` passes locally
- [ ] Tested manually in the browser (`npm run build && npm start`)
- [ ] New behaviour is covered by a test

## Checklist

- [ ] No secrets, tokens or credentials in the diff
- [ ] `package-lock.json` is committed if dependencies changed
- [ ] Workflow changes (if any) have been explained in the description

---

<sub>CI will run automatically. This PR cannot be merged until the **CI passed**
check is green — see `.github/workflows/ci.yml`.</sub>
